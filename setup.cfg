[metadata]
name = api_authorisation_token_scraper
version = 0.0.1

[options]
packages = find:
package_dir =
    =src
python_requires = >=3.9
install_requires =
    selenium >= 3.141.0
    selenium-wire >= 3.0.5

[options.packages.find]
where = src